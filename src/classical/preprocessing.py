from segmentation import BoundingBox


# Load the image from the specified path and return it as a suitable format
def load_image(image_path):
    return None


# Apply preprocessing steps to the input image, such as resizing, normalization, etc.
def preprocess(image):
    return image


# Crop the input image using the provided bounding box and return the cropped image
def crop_character(image, box: BoundingBox):
    return None
