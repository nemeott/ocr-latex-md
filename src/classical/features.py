import numpy as np


# Convert each cropped symbol to a feature vector
def extract_features(symbol_image) -> np.ndarray:
    # HOG, Zoning features, Hu moments, etc?
    features = np.array([0.0])
    return features
